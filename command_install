sudo pacman -S python-virtualenv
python -m venv myenv
source myenv/bin/activate
pip install --upgrade pip
pip install networkx geopandas shapely scikit-learn osmnx matplotlib numpy pandas folium
pip install unidecode
